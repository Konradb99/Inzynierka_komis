export interface PredictionArguments {
    price:number | null,
    year:number | null,
    distanse:number | null,
    capacity:number | null,
    fuelType:string | null,
    bodyType:string | null,
    gearboxType:string | null,
    driveType:string | null
}
